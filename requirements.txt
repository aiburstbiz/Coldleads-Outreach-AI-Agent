# ── Core ──────────────────────────────────────────────────
python-dotenv==1.0.1
pydantic==2.9.2

# ── Agent orchestration ───────────────────────────────────
langgraph>=1.0.0
langchain-core>=1.4.7

# ── LLM ──────────────────────────────────────────────────
groq>=0.9.0
google-genai>=1.0.0

# ── Search ────────────────────────────────────────────────
ddgs>=9.0.0

# ── Scraping (Dev1) ───────────────────────────────────────
playwright==1.47.0
beautifulsoup4==4.12.3

# ── Backend ───────────────────────────────────────────────
fastapi==0.115.0
uvicorn==0.30.6

# ── PPT generation (Dev2) ─────────────────────────────────
python-pptx==1.0.2

# ── Gmail API (Dev2) ──────────────────────────────────────
google-api-python-client==2.146.0
google-auth==2.55.1
google-auth-httplib2==0.2.0
google-auth-oauthlib==1.2.1

# ── Database (Dev2) ───────────────────────────────────────
psycopg2-binary==2.9.9
SQLAlchemy==2.0.35

# ── UI ────────────────────────────────────────────────────
streamlit==1.39.0

# ── Testing ───────────────────────────────────────────────
pytest==8.3.3

# NOTE: transitive/sub-dependencies (langsmith, httpx, orjson, anyio,
# jsonpatch, packaging, requests, urllib3, certifi, typing_extensions,
# etc.) are intentionally NOT pinned here. They come along automatically
# as dependencies of the packages above, and pip resolves compatible
# versions for them on its own. Pinning them to old exact versions is
# what caused the repeated "ResolutionImpossible" conflicts before —
# each one was a stale version left over from before langgraph was
# upgraded, silently fighting the real requirements. Let pip handle
# these unless a specific one genuinely needs to be locked down.